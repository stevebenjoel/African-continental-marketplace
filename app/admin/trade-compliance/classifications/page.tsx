import Link from"next/link";import{requireTradeStaff}from"@/src/modules/authorization/server/require-trade-staff";import{listRegulatoryControl}from"@/src/modules/export-readiness/server/repository";
export const dynamic="force-dynamic";export default async function ClassificationReview({searchParams}:{searchParams:Promise<{reviewed?:string;error?:string}>}){await requireTradeStaff("/admin/trade-compliance/classifications");const[data,q]=await Promise.all([listRegulatoryControl(),searchParams]);return <main className="admin-detail"><header><div><p className="kicker">HS CLASSIFICATION GOVERNANCE</p><h1>Classification review queue</h1><p>Review the recorded facts and reasoning. PAC-SM review is not a customs ruling.</p>{q.reviewed&&<p className="success-note">Classification decision recorded.</p>}{q.error&&<p className="form-error">The decision could not be completed.</p>}</div><Link href="/admin/trade-compliance">Regulatory Control</Link></header><section className="review-table">{data.classifications.map(c=><article className="catalogue-review" key={c.$id}><div><span>{String(c.status).replaceAll("_"," ")}</span><h2>HS6 {String(c.hs6)} · {String(c.confidencePercent)}%</h2><p>Product {String(c.productId)}</p><p>{String(c.reasoning)}</p>{String(c.questions||"[]")!=="[]"&&<small>Clarification questions: {String(c.questions)}</small>}</div>{["AI_SUGGESTED","VENDOR_CONFIRMED"].includes(String(c.status))&&<form action={`/api/admin/trade-compliance/classifications/${c.$id}`} method="post"><label>Decision note<textarea name="notes"/></label><div className="review-actions"><button name="action" value="approve">PAC-SM reviewed</button><button className="danger" name="action" value="reject">Reject classification</button></div></form>}</article>)}</section></main>}
