import Link from"next/link";import{Query}from"node-appwrite";import{requireSuperAdmin}from"@/src/modules/authorization/server/require-super-admin";import{createAppwriteDatabaseClient}from"@/src/integrations/appwrite/server";import{env}from"@/src/shared/config/env";export const dynamic="force-dynamic";export default async function AdminRfqs(){await requireSuperAdmin();const db=createAppwriteDatabaseClient().databases,databaseId=env().APPWRITE_DATABASE_ID,[rfqs,quotes]=await Promise.all([db.listDocuments({databaseId,collectionId:"rfqs",queries:[Query.orderDesc("createdAt"),Query.limit(100)]}),db.listDocuments({databaseId,collectionId:"quotations",queries:[Query.limit(5000)]})]);return <main className="admin-detail"><header><div><p className="kicker">PROCUREMENT OVERSIGHT</p><h1>RFQ marketplace</h1><p>{quotes.total} quotation(s) across {rfqs.total} RFQ(s).</p></div><Link href="/admin">Admin overview</Link></header><section className="review-table">{rfqs.documents.map(r=><div className="review-row" key={r.$id}><span>{String(r.rfqNumber)}</span><strong>{String(r.status)}</strong><span>{String(r.title)}</span><span>{quotes.documents.filter(q=>q.rfqId===r.$id).length} quotations</span></div>)}</section></main>}
