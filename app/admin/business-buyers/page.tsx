import Link from"next/link";import{requireSuperAdmin}from"@/src/modules/authorization/server/require-super-admin";import{listBusinessBuyers}from"@/src/modules/wholesale/server/repository";export const dynamic="force-dynamic";export default async function BusinessBuyers(){await requireSuperAdmin();const buyers=await listBusinessBuyers();return <main className="admin-detail"><header><div><p className="kicker">WHOLESALE COMPLIANCE</p><h1>Business buyers</h1></div><Link href="/admin">Admin overview</Link></header><section className="review-table">{buyers.documents.map(b=><div className="review-row" key={b.$id}><span>{String(b.organizationName)}</span><strong>{String(b.status)}</strong><span>{String(b.countryCode)} · {String(b.buyerType)}</span>{b.status==="pending_review"?<form action={`/api/admin/business-buyers/${b.$id}/review`} method="post"><button name="action" value="approve">Approve</button><button name="action" value="reject">Reject</button></form>:<span/>}</div>)}</section></main>}
