import Link from"next/link";import{requireSuperAdmin}from"@/src/modules/authorization/server/require-super-admin";import{listStockCounts}from"@/src/modules/inventory/server/stocktakes";export const dynamic="force-dynamic";export default async function InventoryAdmin({searchParams}:{searchParams:Promise<{updated?:string;error?:string}>}){await requireSuperAdmin();const[counts,q]=await Promise.all([listStockCounts(),searchParams]);return <main className="admin-detail"><header><div><p className="kicker">INVENTORY GOVERNANCE</p><h1>Physical count approvals</h1><p>Approve a variance only when the submitted evidence reconciles to the physical warehouse count.</p>{q.updated&&<p className="success-note">Count decision recorded and approved variance posted to the ledger.</p>}{q.error&&<p className="form-error">The count could not be approved. Stock may have changed since it was counted.</p>}</div><Link href="/admin">Admin overview</Link></header><section className="review-table"><div className="review-row review-head"><span>Count</span><span>Expected / counted</span><span>Variance</span><span>Decision</span></div>{counts.documents.map(x=><article className="review-row" key={x.$id}><span><strong>{String(x.offerId)}</strong><small>{String(x.reason)}</small></span><span>{String(x.expectedQuantity)} / {String(x.countedQuantity)}</span><strong>{Number(x.variance)>0?`+${x.variance}`:String(x.variance)}</strong>{x.status==="pending_approval"?<form action={`/api/admin/inventory/counts/${x.$id}`} method="post"><input name="notes" minLength={10} required placeholder="Review evidence and reason"/><button name="action" value="approve">Approve adjustment</button><button name="action" value="reject">Reject</button></form>:<span>{String(x.status).replaceAll("_"," ")}</span>}</article>)}</section></main>}
