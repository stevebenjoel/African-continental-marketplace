import Link from "next/link"; import { requireSuperAdmin } from "@/src/modules/authorization/server/require-super-admin"; import { listCoupons } from "@/src/modules/promotions/server/coupons";
export const dynamic = "force-dynamic";
export default async function AdminCouponsPage() { await requireSuperAdmin(); const coupons = await listCoupons(); return <main className="admin-detail"><header><div><p className="kicker">PROMOTIONS</p><h1>Coupon management</h1></div><Link href="/admin">Admin overview</Link></header><section className="catalogue-create"><h2>Create coupon</h2><form action="/api/admin/coupons" method="post"><label>Code<input name="code" required pattern="[A-Za-z0-9_-]{3,40}"/></label><label>Type<select name="discountType"><option value="percentage">Percentage</option><option value="fixed">Fixed NGN</option></select></label><label>Value<input name="value" type="number" min="1" required/><small>Percentage uses %, fixed uses NGN.</small></label><label>Minimum order (NGN)<input name="minimumOrder" type="number" min="0" defaultValue="0"/></label><label>Maximum uses<input name="maximumUses" type="number" min="1" defaultValue="100"/></label><label>Starts<input name="startsAt" type="datetime-local" required/></label><label>Ends<input name="endsAt" type="datetime-local" required/></label><button type="submit">Create coupon</button></form></section><section className="review-table">{coupons.documents.map(coupon => <div className="review-row" key={coupon.$id}><strong>{String(coupon.code)}</strong><span>{String(coupon.discountType)}</span><span>{coupon.discountType === "percentage" ? `${Number(coupon.value)/100}%` : `NGN ${Number(coupon.value)/100}`}</span><span>{String(coupon.uses)} / {String(coupon.maximumUses)} uses</span></div>)}</section></main>; }
