import Link from"next/link";import{requireGlobalCommerceStaff}from"@/src/modules/authorization/server/require-global-commerce-staff";import{listImportedSupplierProducts}from"@/src/modules/global-commerce/server/repository";export const dynamic="force-dynamic";
export default async function ImportedProducts({searchParams}:{searchParams:Promise<{created?:string;mediaWarning?:string}>}){const{canManagePricing}=await requireGlobalCommerceStaff();const[rows,query]=await Promise.all([listImportedSupplierProducts(),searchParams]);return <main className="admin-detail global-commerce-admin"><header><div><p className="kicker">CONTROLLED IMPORT REGISTER</p><h1>Imported global products</h1><p>Source mappings owned commercially by Diplomats Stores. Imported drafts remain unavailable to customers until pricing and catalogue approval are complete.</p></div><Link href="/admin/global-commerce">Global Commerce</Link></header>{query.created&&<p className="success-note">Product imported into the PAC-SM review queue.{query.mediaWarning?" One or more supplier images need manual attention.":" Supplier images were copied to PAC-SM storage."}</p>}<section className="global-import-list">{rows.documents.length?rows.documents.map(row=><article key={row.$id}><div><span>{String(row.status).replaceAll("_"," ")}</span><h2>{String(row.supplierTitle)}</h2><p>{String(row.externalProductSku||row.externalProductId)}</p></div><dl><div><dt>PAC-SM product</dt><dd>{String(row.pacsmProductId)}</dd></div><div><dt>Source cost</dt><dd>{row.supplierCostMinor===undefined?"Unavailable":new Intl.NumberFormat("en-US",{style:"currency",currency:String(row.sourceCurrency)}).format(Number(row.supplierCostMinor)/100)}</dd></div><div><dt>Price sync</dt><dd>{row.priceSyncEnabled?"Enabled":"Disabled"}</dd></div><div><dt>Content sync</dt><dd>{row.contentSyncEnabled?"Enabled":"PAC-SM controlled"}</dd></div></dl>{canManagePricing&&<Link href={`/admin/global-commerce/pricing/${row.$id}`}>Calculate landed price →</Link>}<Link href="/admin/catalogue">Open catalogue review →</Link></article>):<p className="empty-state">No global products have been imported.</p>}</section></main>}
