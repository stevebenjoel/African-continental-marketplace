import Link from "next/link";
import { requireSuperAdmin } from "@/src/modules/authorization/server/require-super-admin";
import { listAllNegotiations } from "@/src/modules/wholesale/server/negotiations";
import { canSuperAdminRespondForVendor } from "@/src/modules/wholesale/domain/negotiation";
export const dynamic = "force-dynamic";
export default async function AdminWholesaleNegotiations() { await requireSuperAdmin(); const rows = await listAllNegotiations(); return <main className="admin-detail"><header><div><p className="kicker">WHOLESALE GOVERNANCE</p><h1>Price negotiation monitor</h1><p>Review every negotiation. Super Admin response controls are limited to PAC-SM-owned and demo inventory.</p></div><Link href="/admin">Back to overview</Link></header><section className="review-table"><div className="review-row review-head"><span>Reference</span><span>Product</span><span>Status</span><span>Action</span></div>{rows.documents.map(row => <div className="review-row" key={row.$id}><span>{String(row.negotiationNumber)}</span><strong>{String(row.productName)}</strong><span>{String(row.status).replaceAll("_", " ")}</span>{canSuperAdminRespondForVendor(String(row.vendorId)) ? <Link href={`/admin/wholesale-negotiations/${row.$id}`}>Review & respond →</Link> : <span>Independent seller</span>}</div>)}</section></main>; }
