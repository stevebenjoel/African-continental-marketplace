import Link from "next/link";
import {requireSuperAdmin} from "@/src/modules/authorization/server/require-super-admin";
import {listAllSupportChannels} from "@/src/modules/support/server/repository";
export const dynamic="force-dynamic";
export default async function AdminSupportPage({searchParams}:{searchParams:Promise<{saved?:string;error?:string}>}){await requireSuperAdmin();const[channels,query]=await Promise.all([listAllSupportChannels(),searchParams]);return <main className="admin-detail"><header><div><p className="kicker">CUSTOMER SUPPORT</p><h1>Live chat and WhatsApp</h1><p>The AI live chat is an in-app assistant. WhatsApp numbers are separate human-support contact channels.</p>{query.saved&&<p className="success-note">Support channels updated.</p>}{query.error&&<p className="form-error">The support channel could not be saved. Confirm the database collection is provisioned and the number is valid.</p>}</div><Link href="/admin">Admin overview</Link></header><section className="catalogue-create"><h2>Add another WhatsApp number</h2><form action="/api/admin/support/channels" method="post"><label>Contact label<input name="label" required maxLength={120} placeholder="Orders and delivery"/></label><label>WhatsApp number<input name="number" required inputMode="tel" placeholder="+234..."/></label><label>Opening message<textarea name="message" maxLength={500} defaultValue="Hello PAC-SM, I need assistance."/></label><label>Display order<input name="sortOrder" type="number" min="1" max="1000" defaultValue="10"/></label><button>Add WhatsApp channel</button></form></section><section className="review-table"><div className="review-row review-head"><span>Contact</span><span>Number</span><span>Status</span><span>Action</span></div>{channels.map(channel=><div className="review-row" key={channel.id}><strong>{channel.label}</strong><span>+{channel.whatsappNumber}</span><span>{channel.status}</span>{channel.id==="primary"?<small>Provision the support collection to manage the default contact.</small>:<form action={`/api/admin/support/channels/${channel.id}`} method="post"><input type="hidden" name="status" value={channel.status==="active"?"inactive":"active"}/><button>{channel.status==="active"?"Disable":"Enable"}</button></form>}</div>)}</section></main>}
