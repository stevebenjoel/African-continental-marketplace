import Link from "next/link";
import { requireSuperAdmin } from "@/src/modules/authorization/server/require-super-admin";
import { listReturnRequests } from "@/src/modules/finance/server/returns";
export const dynamic = "force-dynamic";
export default async function AdminReturnsPage() { await requireSuperAdmin(); const requests = await listReturnRequests(); return <main className="admin-detail"><header><div><p className="kicker">BUYER PROTECTION</p><h1>Return and refund review</h1></div><Link href="/admin">Admin overview</Link></header><section className="review-table"><div className="review-row review-head"><span>Request</span><span>Status</span><span>Reason</span><span>Decision</span></div>{requests.documents.length ? requests.documents.map(request => <div className="review-row" key={request.$id}><span>{request.$id}</span><strong>{String(request.status).replaceAll("_", " ")}</strong><span>{String(request.reason)}</span>{request.status === "requested" ? <form className="inline-actions" action={`/api/admin/returns/${request.$id}/review`} method="post"><input name="notes" placeholder="Decision notes" maxLength={1000}/><button name="action" value="approve">Approve refund</button><button name="action" value="reject">Reject</button></form> : <span>{String(request.reviewNotes ?? "Reviewed")}</span>}</div>) : <p className="empty-state">No return requests are awaiting review.</p>}</section></main>; }
