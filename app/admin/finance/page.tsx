import Link from "next/link";
import { Query } from "node-appwrite";
import { requireSuperAdmin } from "@/src/modules/authorization/server/require-super-admin";
import { createAppwriteDatabaseClient } from "@/src/integrations/appwrite/server";
import { env } from "@/src/shared/config/env";
export const dynamic = "force-dynamic";
export default async function AdminFinancePage() { await requireSuperAdmin(); const databases = createAppwriteDatabaseClient().databases, databaseId = env().APPWRITE_DATABASE_ID; const [entries, refunds, rules] = await Promise.all([databases.listDocuments({ databaseId, collectionId: "journal_entries", queries: [Query.orderDesc("postedAt"), Query.limit(100)] }), databases.listDocuments({ databaseId, collectionId: "refunds", queries: [Query.orderDesc("createdAt"), Query.limit(100)] }), databases.listDocuments({ databaseId, collectionId: "commission_rules", queries: [Query.equal("status", "active"), Query.limit(1)] })]); return <main className="admin-detail"><header><div><p className="kicker">FINANCIAL CONTROL</p><h1>Marketplace ledger</h1><p>Default commission: {Number(rules.documents[0]?.rateBasisPoints ?? 0) / 100}% · <Link href="/admin/settlements">Manage settlements →</Link></p></div><Link href="/admin">Admin overview</Link></header><section className="review-table"><div className="review-row review-head"><span>Posted</span><span>Reference</span><span>Description</span><span>Currency</span></div>{entries.documents.length ? entries.documents.map(entry => <div className="review-row" key={entry.$id}><span>{new Date(String(entry.postedAt)).toLocaleDateString("en-GB")}</span><strong>{String(entry.referenceType)}</strong><span>{String(entry.description)}</span><span>{String(entry.currency)}</span></div>) : <p className="empty-state">No financial journals have been posted.</p>}</section><section className="admin-notice"><strong>{refunds.total} processed refund(s)</strong><p>Refunds and payouts are simulated until licensed payment and banking providers are connected.</p></section></main>; }
