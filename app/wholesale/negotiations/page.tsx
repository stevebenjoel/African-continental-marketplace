import Link from "next/link";
import { redirect } from "next/navigation";
import { getCurrentAppwriteUser } from "@/src/modules/auth/server/session";
import { listBuyerNegotiations } from "@/src/modules/wholesale/server/negotiations";
export const dynamic = "force-dynamic";
export default async function BuyerNegotiations() { const user = await getCurrentAppwriteUser(); if (!user) redirect("/login?returnTo=/wholesale/negotiations"); const rows = await listBuyerNegotiations(user.$id); return <main className="marketplace-shell"><nav><Link className="brand" href="/"><span>PAC</span><b>SM</b></Link><Link href="/wholesale">Wholesale marketplace</Link></nav><header><p className="kicker">WHOLESALE NEGOTIATIONS</p><h1>Your negotiated-price requests</h1><p>Review seller decisions, respond to counteroffers, and move accepted prices into checkout.</p></header><section className="review-table"><div className="review-row review-head"><span>Reference</span><span>Product</span><span>Status</span><span>Action</span></div>{rows.documents.length ? rows.documents.map(row => <div className="review-row" key={row.$id}><span>{String(row.negotiationNumber)}</span><strong>{String(row.productName)}</strong><span>{String(row.status).replaceAll("_", " ")}</span><Link href={`/wholesale/negotiations/${row.$id}`}>Open →</Link></div>) : <p className="empty-state">No negotiations have been started.</p>}</section></main>; }
