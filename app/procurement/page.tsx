import Link from"next/link";import{redirect}from"next/navigation";import{getCurrentAppwriteUser}from"@/src/modules/auth/server/session";import{findBusinessBuyer}from"@/src/modules/wholesale/server/repository";import{listBuyerRfqs}from"@/src/modules/procurement/server/repository";import{listCategories}from"@/src/modules/catalogue/server/repository";export const dynamic="force-dynamic";export default async function ProcurementPage(){const user=await getCurrentAppwriteUser();if(!user)redirect("/login?returnTo=/procurement");const[buyer,rfqs,categories]=await Promise.all([findBusinessBuyer(user.$id),listBuyerRfqs(user.$id),listCategories()]);return <main className="marketplace-shell"><nav><Link className="brand" href="/"><span>PAC</span><b>SM</b></Link><Link href="/wholesale">Wholesale</Link></nav><header><p className="kicker">RFQ PROCUREMENT</p><h1>Source at continental scale.</h1><p>Create a structured request and compare matched supplier quotations.</p></header>{buyer?.status==="approved"?<section className="cart-vendor"><h2>Create request for quotation</h2><form className="stack-form" action="/api/procurement/rfqs" method="post"><label>Title<input name="title" required minLength={5}/></label><label>Requirement<textarea name="description" required minLength={20}/></label><label>Category<select name="categoryId">{categories.documents.map(c=><option value={c.$id} key={c.$id}>{String(c.name)}</option>)}</select></label><label>Quantity<input name="quantity" type="number" min="1" required/></label><label>Unit<input name="unit" placeholder="units, cartons, tonnes" required/></label><label>Delivery country<input name="deliveryCountryCode" minLength={2} maxLength={2} required/></label><label>Currency<input name="currency" defaultValue="NGN" minLength={3} maxLength={3} required/></label><label>Quotation deadline<input name="quotationDeadline" type="datetime-local" required/></label><button>Publish RFQ</button></form></section>:<section className="admin-notice"><strong>Approved business buyer required</strong><p>Register or monitor approval in the <Link href="/wholesale">wholesale centre</Link>.</p></section>}<section className="review-table"><div className="review-row review-head"><span>RFQ</span><span>Status</span><span>Quantity</span><span>Compare</span></div>{rfqs.documents.map(r=><div className="review-row" key={r.$id}><span>{String(r.rfqNumber)} · {String(r.title)}</span><strong>{String(r.status)}</strong><span>{String(r.quantity)} {String(r.unit)}</span><Link href={`/procurement/${r.$id}`}>View quotations →</Link></div>)}</section></main>}
