import Link from "next/link";
import { requireAcademyLearner } from "@/src/modules/academy/server/access";
import { getAcademyDashboard,isAcademyModuleUnlocked } from "@/src/modules/academy/server/repository";
import { ACADEMY_COURSE,ACADEMY_MODULES,academyLessonCount } from "@/src/modules/academy/domain/curriculum";
export const dynamic="force-dynamic";
export default async function AcademyPage({searchParams}:{searchParams:Promise<{enrolled?:string}>}){const user=await requireAcademyLearner(),[dashboard,query]=await Promise.all([getAcademyDashboard(user.$id),searchParams]);if(!dashboard.enrollment)return <main className="academy-shell"><nav className="academy-nav"><Link className="brand" href="/"><span>PAC</span><b>SM</b></Link><Link href="/account">My account</Link></nav><section className="academy-welcome"><div><p className="kicker">PAC-SM TRADE & EXPORT ACADEMY</p><h1>Learn. Prepare. Connect. Trade. Scale.</h1><p>Build practical competence across African trade, product readiness, documentation, packaging, food safety, pricing, logistics, finance and buyer acquisition.</p><div className="academy-facts"><span><strong>10</strong> modules</span><span><strong>{ACADEMY_COURSE.estimatedHours}</strong> guided hours</span><span><strong>{academyLessonCount}</strong> structured lessons</span></div><form action="/api/academy/enroll" method="post"><button>Start the Academy</button></form></div><aside><span>YOUR FINAL CHALLENGE</span><h2>Develop a complete export plan for one African product or service.</h2><p>Build the project progressively from product and market selection through compliance, pricing, logistics, payment, finance, sales and risk management.</p></aside></section></main>;const nextModule=ACADEMY_MODULES.find(module=>isAcademyModuleUnlocked(module.id,dashboard.completedLessonIds,dashboard.passedModuleIds)&&module.lessons.some(lesson=>!dashboard.completedLessonIds.has(lesson.id)))??ACADEMY_MODULES.at(-1)!;return <main className="academy-shell"><nav className="academy-nav"><Link className="brand" href="/"><span>PAC</span><b>SM</b></Link><div><Link href="/academy/project">Capstone project</Link><Link href="/documentation#academy">Academy guide</Link><Link href="/account">My account</Link></div></nav><header className="academy-dashboard-hero"><div><p className="kicker">WELCOME TO YOUR ACADEMY</p><h1>{ACADEMY_COURSE.title}</h1><p>{user.name}, continue building your practical continental trade capability.</p>{query.enrolled&&<p className="academy-success">Your enrolment is active. Module 1 is ready.</p>}<Link href={`/academy/modules/${nextModule.id}`}>Continue learning →</Link></div><div className="academy-progress-ring" style={{"--progress":`${dashboard.progressPercent*3.6}deg`} as React.CSSProperties}><strong>{dashboard.progressPercent}%</strong><span>coursework</span></div></header><section className="academy-stat-grid"><article><span>LESSONS COMPLETE</span><strong>{dashboard.completedCount}/{academyLessonCount}</strong></article><article><span>ASSESSMENTS PASSED</span><strong>{dashboard.passedModuleIds.size}/10</strong></article><article><span>PASS STANDARD</span><strong>{ACADEMY_COURSE.passMark}%</strong></article><article><span>STATUS</span><strong>{String(dashboard.enrollment.status).replaceAll("_"," ")}</strong></article></section><section className="academy-module-list"><div className="academy-section-heading"><div><span>YOUR ROADMAP</span><h2>Ten practical trade modules</h2></div><p>Pass each module assessment to unlock the next module.</p></div>{ACADEMY_MODULES.map(module=>{const unlocked=isAcademyModuleUnlocked(module.id,dashboard.completedLessonIds,dashboard.passedModuleIds),done=module.lessons.filter(lesson=>dashboard.completedLessonIds.has(lesson.id)).length,passed=dashboard.passedModuleIds.has(module.id);return <article className={unlocked?"unlocked":"locked"} key={module.id}><span className="academy-module-number">{String(module.number).padStart(2,"0")}</span><div><small>{module.durationHours} HOURS · {module.lessons.length} LESSONS</small><h3>{module.title}</h3><p>{module.purpose}</p><div className="academy-module-meter"><i style={{width:`${done/module.lessons.length*100}%`}}/></div><small>{done}/{module.lessons.length} completed · {passed?"assessment passed":"assessment pending"}</small></div>{unlocked?<Link href={done===module.lessons.length&&!passed?`/academy/assessments/${module.id}`:`/academy/modules/${module.id}`}>{done===module.lessons.length&&!passed?"Take assessment":"Open module"} →</Link>:<b>Locked</b>}</article>})}</section></main>}
