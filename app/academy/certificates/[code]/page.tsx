import Link from "next/link";
import { notFound } from "next/navigation";
import { verifyAcademyCertificate } from "@/src/modules/academy/server/project-repository";
import { ACADEMY_COURSE } from "@/src/modules/academy/domain/curriculum";
export const dynamic="force-dynamic";
export default async function CertificatePage({params}:{params:Promise<{code:string}>}){const{code}=await params,certificate=await verifyAcademyCertificate(code);if(!certificate)notFound();return <main className="academy-shell academy-credential"><nav className="academy-nav"><Link className="brand" href="/"><span>PAC</span><b>SM</b></Link><Link href="/academy">Trade Academy</Link></nav><article><p>PAC-SM TRADE & EXPORT ACADEMY</p><h1>Verified programme credential</h1><span>This record confirms that</span><h2>{String(certificate.learnerName)}</h2><p>completed the coursework, protected module assessments and practical export-plan project for</p><h3>{ACADEMY_COURSE.title}</h3><dl><div><dt>Certificate</dt><dd>{String(certificate.certificateNumber)}</dd></div><div><dt>Programme version</dt><dd>{String(certificate.courseVersion)}</dd></div><div><dt>Issued</dt><dd>{new Date(String(certificate.issuedAt)).toLocaleDateString("en-GB")}</dd></div><div><dt>Status</dt><dd>{String(certificate.status)}</dd></div></dl><small>This PAC-SM programme credential does not replace a government licence or regulated professional certification.</small></article></main>}
