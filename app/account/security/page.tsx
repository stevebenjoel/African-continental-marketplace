import Link from "next/link";
import { redirect } from "next/navigation";
import { getCurrentAppwriteUser } from "@/src/modules/auth/server/session";
export const dynamic = "force-dynamic";
export default async function AccountSecurity({ searchParams }: { searchParams: Promise<{ changed?: string; error?: string }> }) { const user = await getCurrentAppwriteUser(); if (!user) redirect("/login?returnTo=/account/security"); const query = await searchParams; return <main className="account-shell"><nav className="account-nav"><Link className="brand" href="/"><span>PAC</span><b>SM</b></Link><Link href="/account">Account</Link></nav><section className="account-card"><p className="kicker">ACCOUNT SECURITY</p><h1>Change password.</h1><p>Assisted-onboarding sellers should replace their temporary password immediately after first sign-in.</p>{query.changed && <p className="success-note">Password changed successfully.</p>}{query.error && <p className="form-error">Password change failed. Confirm the current password and use a strong new password.</p>}<form className="vendor-form" action="/api/account/password" method="post"><label>Current password<input name="currentPassword" type="password" autoComplete="current-password" required/></label><label>New password<input name="newPassword" type="password" autoComplete="new-password" minLength={12} required/></label><button>Change password</button></form></section></main>; }
