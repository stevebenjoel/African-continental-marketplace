import Link from "next/link"; import { listActiveCarriers, listCarrierRates } from "@/src/modules/logistics/server/repository";
export const dynamic = "force-dynamic";
export default async function LogisticsMarketplace() { const [carriers, rates] = await Promise.all([listActiveCarriers(), listCarrierRates()]); return <main className="marketplace-shell"><nav><Link className="brand" href="/"><span>PAC</span><b>SM</b></Link><div><Link href="/track">Track shipment</Link> · <Link href="/carriers/apply">Become a carrier</Link></div></nav><header><p className="kicker">VERIFIED CONTINENTAL LOGISTICS</p><h1>Move goods across Africa.</h1><p>Discover verified carriers, active trade corridors and transparent service rates.</p></header><section className="product-grid">{carriers.documents.map(carrier => { const carrierRates = rates.documents.filter(rate => rate.carrierId === carrier.$id); return <article key={carrier.$id}><div className="product-placeholder">{String(carrier.tradingName ?? carrier.legalName).slice(0, 2).toUpperCase()}</div><span>{String(carrier.carrierNumber)} · {String(carrier.rating)} rating</span><h2>{String(carrier.tradingName ?? carrier.legalName)}</h2><p>{String(carrier.transportModes)} · {(carrier.serviceCountries as string[]).join(", ")}</p><strong>{carrierRates.length} active route{carrierRates.length === 1 ? "" : "s"}</strong>{carrierRates.slice(0, 3).map(rate => <small key={rate.$id}>{String(rate.originCountryCode)} → {String(rate.destinationCountryCode)} · {String(rate.currency)} {(Number(rate.baseRateMinor) / 100).toLocaleString()}</small>)}</article>; })}{!carriers.total && <p className="empty-market">No verified carriers are listed yet.</p>}</section></main>; }
