import Link from"next/link";import{redirect}from"next/navigation";import{Query}from"node-appwrite";import{getCurrentAppwriteUser}from"@/src/modules/auth/server/session";import{findVendorByOwner}from"@/src/modules/vendors/server/repository";import{listRequirements}from"@/src/modules/offtake/server/repository";export const dynamic="force-dynamic";export default async function Page(){const u=await getCurrentAppwriteUser();if(!u)redirect("/login?returnTo=/seller/offtake");const v=await findVendorByOwner(u.$id);if(!v)redirect("/vendor/register");const reqs=await listRequirements([Query.equal("status","published")]);return <main className="seller-shell"><nav><Link className="brand" href="/"><span>PAC</span><b>SM</b></Link><Link href="/seller">Seller Centre</Link></nav><header><div><p className="kicker">OFF-TAKE OPPORTUNITIES</p><h1>Long-term supply demand.</h1></div></header><section className="review-table">{reqs.documents.map(r=><article className="catalogue-review" key={r.$id}><div><span>{String(r.frequency)} · deadline {new Date(String(r.applicationDeadline)).toLocaleDateString("en-GB")}</span><h2>{String(r.title)}</h2><p>{String(r.quantity)} {String(r.unit)} · {String(r.deliveryDestination)}</p><small>{String(r.specifications)}</small></div><form action={`/api/seller/offtake/${r.$id}/proposals`} method="post"><label>Supply quantity<input name="quantity" type="number" min="1" max={Number(r.quantity)} required/></label><label>Unit price ({String(r.currency)})<input name="unitPrice" type="number" min=".01" step=".01" required/></label><label>Lead time days<input name="leadTimeDays" type="number" min="1" required/></label><label>Certifications<textarea name="certifications"/></label><label>Commercial terms<textarea name="terms" minLength={10} required/></label><button>Submit supply proposal</button></form></article>)}</section></main>}
