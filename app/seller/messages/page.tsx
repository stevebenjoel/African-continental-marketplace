import Link from"next/link";import{redirect}from"next/navigation";import{getCurrentAppwriteUser}from"@/src/modules/auth/server/session";import{findVendorByOwner}from"@/src/modules/vendors/server/repository";import{listSellerConversations}from"@/src/modules/messaging/server/repository";
export const dynamic="force-dynamic";export default async function SellerMessages(){const user=await getCurrentAppwriteUser();if(!user)redirect("/login?returnTo=/seller/messages");const vendor=await findVendorByOwner(user.$id);if(!vendor)redirect("/vendor/register");const items=await listSellerConversations(vendor.$id);return <main className="seller-shell"><nav><Link className="brand" href="/"><span>PAC</span><b>SM</b></Link><Link href="/seller">Seller Centre</Link></nav><header><div><p className="kicker">CUSTOMER COMMUNICATIONS</p><h1>Order messages</h1></div></header><section className="review-table">{items.documents.length?items.documents.map(item=><div className="review-row" key={item.$id}><span>{String(item.vendorOrderId)}</span><strong>{String(item.status)}</strong><span>{new Date(String(item.updatedAt)).toLocaleString("en-GB")}</span><Link href={`/messages/${item.$id}`}>Reply →</Link></div>):<p className="empty-state">No customer conversations.</p>}</section></main>}
