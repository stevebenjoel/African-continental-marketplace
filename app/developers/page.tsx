import Link from "next/link";
const endpoints = [{ method: "GET", path: "/api/v1/merchant/products", scope: "products:read" }, { method: "GET", path: "/api/v1/merchant/inventory", scope: "inventory:read" }, { method: "PATCH", path: "/api/v1/merchant/inventory", scope: "inventory:write" }, { method: "GET", path: "/api/v1/merchant/orders", scope: "orders:read" }];
export default function DeveloperPortal() { return <main className="admin-detail"><header><div><p className="kicker">PAC-SM DEVELOPER PLATFORM</p><h1>Merchant API v1</h1><p>REST APIs and signed events for enterprise commerce integrations.</p></div><Link href="/seller/integrations">Integration console</Link></header><section className="admin-notice"><strong>Authentication</strong><p>Send your one-time API key as <code>Authorization: Bearer YOUR_KEY</code>. Keys are scoped, revocable and rate-limited.</p></section><section className="review-table"><div className="review-row review-head"><span>Method</span><span>Endpoint</span><span>Required scope</span><span>Format</span></div>{endpoints.map(endpoint => <div className="review-row" key={`${endpoint.method}-${endpoint.path}`}><strong>{endpoint.method}</strong><code>{endpoint.path}</code><span>{endpoint.scope}</span><span>application/json</span></div>)}</section><section className="catalogue-create"><h2>Inventory update</h2><pre>{`PATCH /api/v1/merchant/inventory\nAuthorization: Bearer pacsm_live_...\nContent-Type: application/json\n\n{"balanceId":"...","onHand":125}`}</pre><h2>Webhook verification</h2><p>Calculate HMAC-SHA256 over the exact request body using your endpoint signing secret, then compare it to <code>X-PACSM-Signature</code>. Deduplicate using <code>X-PACSM-Event-Id</code>.</p></section></main>; }
