import{getCurrentAppwriteUser}from"@/src/modules/auth/server/session";import{assertSameOrigin}from"@/src/modules/auth/server/request-security";import{registerBusinessBuyer}from"@/src/modules/wholesale/server/repository";export async function POST(request:Request){try{assertSameOrigin(request)}catch{return new Response("Forbidden",{status:403})}const user=await getCurrentAppwriteUser();if(!user)return new Response("Unauthorized",{status:401});const f=await request.formData();await registerBusinessBuyer({userId:user.$id,organizationName:String(f.get("organizationName")),registrationNumber:String(f.get("registrationNumber")),countryCode:String(f.get("countryCode")),buyerType:String(f.get("buyerType"))});return Response.redirect(new URL("/wholesale?registered=1",request.url),303)}
