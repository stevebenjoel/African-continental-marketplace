import{getCurrentAppwriteUser}from"@/src/modules/auth/server/session";import {assertSameOrigin, publicAppUrl} from "@/src/modules/auth/server/request-security";import{applyOfftaker}from"@/src/modules/offtake/server/repository";export async function POST(r:Request){try{assertSameOrigin(r)}catch{return new Response("Forbidden",{status:403})}const u=await getCurrentAppwriteUser();if(!u)return new Response("Unauthorized",{status:401});const f=await r.formData(),d=Object.fromEntries([...f.entries()].map(([k,v])=>[k,String(v).trim()]));try{await applyOfftaker(d,u.$id);return Response.redirect(publicAppUrl("/offtakers?submitted=1"),303)}catch{return Response.redirect(publicAppUrl("/offtakers/register?error=1"),303)}}
