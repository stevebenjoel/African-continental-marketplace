import {assertSameOrigin, publicAppUrl} from "@/src/modules/auth/server/request-security";import{requireTradeStaff}from"@/src/modules/authorization/server/require-trade-staff";import{isCertificationReviewAction}from"@/src/modules/standards/domain/compliance";import{reviewCertificationApplication}from"@/src/modules/standards/server/repository";
export async function POST(request:Request,{params}:{params:Promise<{applicationId:string}>}){try{assertSameOrigin(request)}catch{return new Response("Forbidden",{status:403})}const user=await requireTradeStaff("/admin/standards"),form=await request.formData(),action=String(form.get("action"));if(!isCertificationReviewAction(action))return new Response("Invalid action",{status:400});await reviewCertificationApplication((await params).applicationId,action,String(form.get("notes")??""),user.$id);return Response.redirect(publicAppUrl("/admin/standards?reviewed=1"),303)}
