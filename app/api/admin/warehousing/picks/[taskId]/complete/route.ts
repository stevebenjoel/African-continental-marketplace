import{getCurrentAppwriteUser}from"@/src/modules/auth/server/session";import{assertSameOrigin}from"@/src/modules/auth/server/request-security";import{hasWarehousePermission}from"@/src/modules/authorization/domain/admin-roles";import{completePick}from"@/src/modules/warehousing/server/repository";export async function POST(request:Request,{params}:{params:Promise<{taskId:string}>}){try{assertSameOrigin(request)}catch{return new Response("Forbidden",{status:403})}const user=await getCurrentAppwriteUser();if(!user||!hasWarehousePermission(user.labels,"pick"))return new Response("Forbidden",{status:403});const{taskId}=await params;await completePick(taskId,user.$id);return Response.redirect(new URL("/admin/warehousing?picked=1",request.url),303)}
