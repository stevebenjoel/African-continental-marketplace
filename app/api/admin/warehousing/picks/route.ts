import{getCurrentAppwriteUser}from"@/src/modules/auth/server/session";import {assertSameOrigin, publicAppUrl} from "@/src/modules/auth/server/request-security";import{hasWarehousePermission}from"@/src/modules/authorization/domain/admin-roles";import{createPickTasks}from"@/src/modules/warehousing/server/repository";export async function POST(request:Request){try{assertSameOrigin(request)}catch{return new Response("Forbidden",{status:403})}const user=await getCurrentAppwriteUser();if(!user||!hasWarehousePermission(user.labels,"create_pick"))return new Response("Forbidden",{status:403});const form=await request.formData();await createPickTasks(String(form.get("vendorOrderId")));return Response.redirect(publicAppUrl("/admin/warehousing?picks=1"),303)}
