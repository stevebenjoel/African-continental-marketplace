import { getCurrentAppwriteUser } from "@/src/modules/auth/server/session";
import { assertSameOrigin } from "@/src/modules/auth/server/request-security";
import { isSuperAdmin } from "@/src/modules/authorization/domain/super-admin";
import { deleteAdminProduct, renameAdminProduct } from "@/src/modules/catalogue/server/repository";
export async function POST(request: Request, { params }: { params: Promise<{ productId: string }> }) { try { assertSameOrigin(request); } catch { return new Response("Forbidden", { status: 403 }); } const user = await getCurrentAppwriteUser(); if (!user || !isSuperAdmin(user.labels)) return new Response("Forbidden", { status: 403 }); const [{ productId }, form] = await Promise.all([params, request.formData()]); const action = String(form.get("action")); try { if (action === "rename") await renameAdminProduct(productId, String(form.get("name") ?? ""), user.$id); else if (action === "delete") await deleteAdminProduct(productId, user.$id); else return new Response("Invalid action", { status: 400 }); return Response.redirect(new URL("/admin/catalogue?updated=1", request.url), 303); } catch (error) { console.error("Admin product management failed", error); return Response.redirect(new URL("/admin/catalogue?error=1", request.url), 303); } }
