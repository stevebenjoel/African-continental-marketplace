import { assertSameOrigin } from "@/src/modules/auth/server/request-security";
import { requireTradeStaff } from "@/src/modules/authorization/server/require-trade-staff";
import { updateCustomsStatus } from "@/src/modules/trade/server/repository";
export async function POST(request: Request, { params }: { params: Promise<{ shipmentId: string }> }) { try { assertSameOrigin(request); } catch { return new Response("Forbidden", { status: 403 }); } const user = await requireTradeStaff(), form = await request.formData(), { shipmentId } = await params; try { await updateCustomsStatus(shipmentId, String(form.get("customsStatus") ?? ""), String(form.get("customsReference") ?? "").trim(), user.$id); return Response.redirect(new URL(String(request.headers.get("referer") ?? "/admin/trade"), request.url), 303); } catch { return new Response("Invalid customs update", { status: 400 }); } }
