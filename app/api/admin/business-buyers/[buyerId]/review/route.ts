import{getCurrentAppwriteUser}from"@/src/modules/auth/server/session";import {assertSameOrigin, publicAppUrl} from "@/src/modules/auth/server/request-security";import{isSuperAdmin}from"@/src/modules/authorization/domain/super-admin";import{reviewBusinessBuyer}from"@/src/modules/wholesale/server/repository";export async function POST(request:Request,{params}:{params:Promise<{buyerId:string}>}){try{assertSameOrigin(request)}catch{return new Response("Forbidden",{status:403})}const user=await getCurrentAppwriteUser();if(!user||!isSuperAdmin(user.labels))return new Response("Forbidden",{status:403});const[{buyerId},f]=await Promise.all([params,request.formData()]);await reviewBusinessBuyer(buyerId,String(f.get("action"))==="approve",user.$id);return Response.redirect(publicAppUrl("/admin/business-buyers"),303)}
