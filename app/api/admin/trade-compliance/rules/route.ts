import{assertSameOrigin,publicAppUrl}from"@/src/modules/auth/server/request-security";import{requireTradeStaff}from"@/src/modules/authorization/server/require-trade-staff";import{createRegulatorySourceAndRule}from"@/src/modules/export-readiness/server/repository";
export async function POST(r:Request){try{assertSameOrigin(r)}catch{return new Response("Forbidden",{status:403})}const u=await requireTradeStaff("/admin/trade-compliance"),f=await r.formData(),data=Object.fromEntries(Array.from(f.entries()).map(([k,v])=>[k,String(v).trim()]));try{await createRegulatorySourceAndRule(data,u.$id);return Response.redirect(publicAppUrl("/admin/trade-compliance?created=1"),303)}catch(e){console.error("Regulatory rule publishing failed",e);return Response.redirect(publicAppUrl("/admin/trade-compliance?error=1"),303)}}
