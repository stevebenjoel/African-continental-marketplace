import { CommerceModuleHeader, CommerceModuleHero } from "@/app/components/commerce-module-header";
import { trackShipment } from "@/src/modules/logistics/server/repository";
export const dynamic = "force-dynamic";
export default async function TrackingPage({searchParams}:{searchParams:Promise<{number?:string}>}) { const query=await searchParams, number=String(query.number??"").trim(), tracking=number?await trackShipment(number):null;
  return <main className="commerce-module"><CommerceModuleHeader current="logistics" utility={{href:"/logistics",label:"Carrier network"}}/><CommerceModuleHero tone="blue" eyebrow="CONTINENTAL SHIPMENT TRACKING" title="Track your delivery." description="Enter the PAC-SM tracking number to see verified fulfilment, transit and delivery events."><form className="module-hero-search" action="/track" method="get"><input name="number" defaultValue={number} placeholder="Enter tracking number" aria-label="Tracking number" required/><button>Track shipment</button></form></CommerceModuleHero><div className="module-body">{number&&!tracking&&<p className="form-error">No shipment was found for that tracking number.</p>}{tracking?<section className="module-panel"><div className="module-panel-heading"><div><span>SHIPMENT {String(tracking.shipment.trackingNumber)}</span><h2>{String(tracking.shipment.status).replaceAll("_"," ")}</h2><p>Every event below is recorded in chronological shipment history.</p></div></div><div className="module-data-list">{tracking.events.map(event=><article className="module-data-card" key={event.$id}><div><span>{String(event.status).replaceAll("_"," ")}</span><h3>{String(event.location??"In transit")}</h3><p>{String(event.message)}</p></div><strong>{new Date(String(event.occurredAt)).toLocaleString("en-GB")}</strong></article>)}</div>{tracking.proof&&<p className="module-success">Delivered to {String(tracking.proof.recipientName)} on {new Date(String(tracking.proof.deliveredAt)).toLocaleString("en-GB")}.</p>}</section>:!number&&<section className="module-feature-strip"><article><span>01</span><div><strong>Order confirmed</strong><small>Payment and stock secured</small></div></article><article><span>02</span><div><strong>Picked and packed</strong><small>Warehouse preparation</small></div></article><article><span>03</span><div><strong>In transit</strong><small>Carrier milestone visibility</small></div></article><article><span>04</span><div><strong>Delivered</strong><small>Recipient proof recorded</small></div></article></section>}</div></main>;
}
