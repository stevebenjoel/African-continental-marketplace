export function calculateDiscount(input: { subtotalMinor: number; discountType: "percentage" | "fixed"; value: number; maximumDiscountMinor?: number }): number { const raw = input.discountType === "percentage" ? Math.floor(input.subtotalMinor * input.value / 10_000) : input.value; return Math.max(0, Math.min(input.subtotalMinor, input.maximumDiscountMinor ? Math.min(raw, input.maximumDiscountMinor) : raw)); }
