import "server-only"; import { notFound, redirect } from "next/navigation"; import { getCurrentAppwriteUser } from "@/src/modules/auth/server/session";
export async function requireLogisticsStaff(returnTo = "/admin/logistics") { const user = await getCurrentAppwriteUser(); if (!user) redirect(`/login?returnTo=${encodeURIComponent(returnTo)}`); if (!user.labels.some(label => ["superadmin", "logistics_manager", "logistics_dispatcher"].includes(label))) notFound(); return user; }
