import"server-only";import{ID,Query}from"node-appwrite";import{createAppwriteAuthClient,createAppwriteDatabaseClient}from"@/src/integrations/appwrite/server";import{env}from"@/src/shared/config/env";import{isAdminRole,type AdminRole}from"@/src/modules/authorization/domain/admin-roles";
const databaseId=()=>env().APPWRITE_DATABASE_ID;export const listAdminMembers=()=>createAppwriteDatabaseClient().databases.listDocuments({databaseId:databaseId(),collectionId:"admin_members",queries:[Query.orderDesc("assignedAt"),Query.limit(200)]});
export async function assignAdminRole(email:string,role:AdminRole,actorUserId:string){if(!isAdminRole(role))throw new Error("Invalid role");const normalized=email.trim().toLowerCase(),users=createAppwriteAuthClient().users,result=await users.list({queries:[Query.equal("email",normalized),Query.limit(1)]}),user=result.users[0];if(!user)throw new Error("The user must register first");const labels=Array.from(new Set([...user.labels,role]));await users.updateLabels({userId:user.$id,labels});const db=createAppwriteDatabaseClient().databases,existing=await db.listDocuments({databaseId:databaseId(),collectionId:"admin_members",queries:[Query.equal("userId",user.$id),Query.equal("role",role),Query.limit(1)]}),data={userId:user.$id,email:normalized,role,status:"active",assignedBy:actorUserId,assignedAt:new Date().toISOString()};if(existing.documents[0])await db.updateDocument({databaseId:databaseId(),collectionId:"admin_members",documentId:existing.documents[0].$id,data});else await db.createDocument({databaseId:databaseId(),collectionId:"admin_members",documentId:ID.unique(),permissions:[],data});await db.createDocument({databaseId:databaseId(),collectionId:"audit_logs",documentId:ID.unique(),permissions:[],data:{actorUserId,action:"admin_role.assign",entityType:"user",entityId:user.$id,metadata:JSON.stringify({email:normalized,role}),occurredAt:new Date().toISOString()}})}
export async function revokeAdminRole(memberId:string,actorUserId:string){const db=createAppwriteDatabaseClient().databases,member=await db.getDocument({databaseId:databaseId(),collectionId:"admin_members",documentId:memberId});if(member.status!=="active"||!isAdminRole(String(member.role)))throw new Error("Role is not active");const users=createAppwriteAuthClient().users,user=await users.get({userId:String(member.userId)}),labels=user.labels.filter(label=>label!==member.role);await users.updateLabels({userId:user.$id,labels});await db.updateDocument({databaseId:databaseId(),collectionId:"admin_members",documentId:memberId,data:{status:"revoked",revokedAt:new Date().toISOString()}});await db.createDocument({databaseId:databaseId(),collectionId:"audit_logs",documentId:ID.unique(),permissions:[],data:{actorUserId,action:"admin_role.revoke",entityType:"user",entityId:user.$id,metadata:JSON.stringify({role:member.role}),occurredAt:new Date().toISOString()}})}
