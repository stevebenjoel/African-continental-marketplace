export type OrderStockRequirement={offerId:string;quantity:number;purchaseType?:string;fulfilmentMethod?:string};
export const requiresPhysicalInventory=(purchaseType?:string,fulfilmentMethod?:string)=>fulfilmentMethod!=="global_direct"&&!["preorder","white_label"].includes(purchaseType??"standard");
export function reservationsCoverStandardItems(items:OrderStockRequirement[],reservedByOffer:ReadonlyMap<string,number>){return items.filter(item=>requiresPhysicalInventory(item.purchaseType,item.fulfilmentMethod)).every(item=>Number.isSafeInteger(item.quantity)&&item.quantity>0&&(reservedByOffer.get(item.offerId)??0)>=item.quantity)}
