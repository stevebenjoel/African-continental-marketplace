import assert from"node:assert/strict";import{describe,it}from"node:test";import{hasWarehousePermission}from"../../src/modules/authorization/domain/admin-roles.ts";describe("warehouse administrative privileges",()=>{it("keeps warehouse duties separated",()=>{assert.equal(hasWarehousePermission(["warehouse_receiving"],"receive"),true);assert.equal(hasWarehousePermission(["warehouse_receiving"],"pack"),false);assert.equal(hasWarehousePermission(["warehouse_picker"],"pick"),true);assert.equal(hasWarehousePermission(["warehouse_picker"],"create_pick"),false)});it("allows managers and super admins to perform every warehouse action",()=>{assert.equal(hasWarehousePermission(["warehouse_manager"],"pack"),true);assert.equal(hasWarehousePermission(["superadmin"],"create_pick"),true)})});
