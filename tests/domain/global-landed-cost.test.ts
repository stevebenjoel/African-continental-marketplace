import test from "node:test";import assert from "node:assert/strict";import{calculateLandedCost}from"../../src/modules/global-commerce/domain/landed-cost.ts";
const rule={ruleType:"percentage_plus_fixed" as const,markupBasisPoints:2000,fixedMarkupMinor:1000,insuranceBasisPoints:100,dutyBasisPoints:500,taxBasisPoints:750,paymentBasisPoints:150,fxBufferBasisPoints:300,platformCostMinor:500,localDeliveryMinor:2000,otherCostMinor:0,minimumMarginBasisPoints:1500,minimumMarginMinor:500};
test("landed cost includes every configured layer and protects margin",()=>{const p=calculateLandedCost({supplierCostMinor:1000,fxRateMillionths:1_500_000_000,internationalShippingMinor:20000,rule});assert.equal(p.convertedSupplierCostMinor,1_500_000);assert.ok(p.finalPriceMinor>p.landedCostMinor);assert.ok(p.marginBasisPoints>=1500)});
test("minimum absolute margin overrides a weak configured markup",()=>{const p=calculateLandedCost({supplierCostMinor:100,fxRateMillionths:1_000_000,internationalShippingMinor:0,rule:{...rule,ruleType:"minimum_margin",minimumMarginBasisPoints:0,minimumMarginMinor:500}});assert.equal(p.marginMinor,500)});
