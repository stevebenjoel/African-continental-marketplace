import assert from"node:assert/strict";import{describe,it}from"node:test";import{reconcileInbound}from"../../src/modules/warehousing/domain/reconciliation.ts";describe("warehouse inbound reconciliation",()=>{it("tracks shortage and awaiting put-away",()=>assert.deepEqual(reconcileInbound(100,95,80),{shortage:5,awaitingPutaway:15,reconciled:false}));it("rejects put-away above received stock",()=>assert.throws(()=>reconcileInbound(100,90,91)))})
