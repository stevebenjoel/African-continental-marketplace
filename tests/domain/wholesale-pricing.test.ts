import assert from"node:assert/strict";import{describe,it}from"node:test";import{resolveWholesalePrice}from"../../src/modules/wholesale/domain/pricing.ts";describe("wholesale price tiers",()=>{it("uses the most specific eligible tier",()=>{const tiers=[{minimumQuantity:10,unitPriceMinor:900},{minimumQuantity:50,unitPriceMinor:750}];assert.equal(resolveWholesalePrice(60,1000,tiers),750);assert.equal(resolveWholesalePrice(20,1000,tiers),900);assert.equal(resolveWholesalePrice(5,1000,tiers),1000)})});
